import React from "react";
import Hero from "../ui/Hero.jsx";

export default function Header() {
    return (
        <header className="w-full flex items-center justify-center bg-red-400">
            <Hero />
        </header>
    )
}